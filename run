#! usr/bin/env php
<?php

namespace Creational\AbstractFactory;

require_once 'vendor/autoload.php';

$content = 'Message';
$handler = new Handler();

printf("%s \n", $handler->getMessage(new XMLFactory(), $content));
printf("%s \n", $handler->getMessage(new JsonFactory(), $content));
